package uitools.shapes;

import flixel.FlxObject;

class Rectangle extends FlxObject {
  public function new(?width:Int = 100, ?height:Int = 100, ?color:FlxColor = FlxColor.WHITE, ?x:Int = 0, ?y:Int = 0) {
  
  }
}
